use common::Variant;

#[derive(Debug, Clone, Copy, Hash, PartialEq, Eq)]
pub struct StateItem {
    
}

impl StateItem {
    pub fn new(variant: Variant) -> Self {
        Self {
            
        }
    }
}