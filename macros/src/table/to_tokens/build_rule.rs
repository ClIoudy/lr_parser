use std::collections::HashMap;

use common::{NonTerminal, Variant};


pub fn build_rule(rules: HashMap<NonTerminal, Vec<Variant>>) {
    // match symbol
    //  match name
    //   to building variant from given children-values


}